server {
  listen <%= node['repository']['frontend']['listen_port'] %>;
  server_name <%= node['repository']['frontend']['fqdn'] %>;

  access_log <%= node['nginx']['log_dir'] %>/repository.access.log;

  location / {
    root <%= node['repository']['base'] %>;
    index index.html index.htm;
  }
}